# Известные проблемы и подводные камни

## 1. HTML-инъекция из пользовательских тем — НЕ ИСПРАВЛЕНО

<!-- CONFIDENCE: HIGH — воспроизведено вызовом texts.theme_card() 2026-08-12 -->

**Severity: HIGH** — ломается пользователем без злого умысла, обычным символом `&`.

`parse_mode=ParseMode.HTML` глобальный, названия тем и слов подставляются в разметку
без `html.escape()`. Проверено:

```python
>>> texts.theme_card('Мемы <b>крутые', 5, 'вы')
'📗 <b>Мемы <b>крутые</b>\n\nСлов: 5\nАвтор: вы'      # незакрытый тег
>>> texts.editor_word_card('Тим & Ко', ['a<b'])
'🔤 <b>Тим & Ко</b>\n\nПохожие слова:\n• a<b'         # голый & — Telegram отвергнет
```

Telegram вернёт `TelegramBadRequest: can't parse entities`, хендлер упадёт с
необработанным исключением. Пользователь окажется в тупике: тема создана, но её карточка
не открывается.

Затронуты все функции [texts.py](../../src/bond_bot/presentation/texts.py), принимающие
пользовательский текст: `theme_card`, `word_list`, `editor_word_card`, `ask_word`,
`ask_similar`, `confirm_delete`, `added_words`, а также названия тем и слов в кнопках
[keyboards/themes.py](../../src/bond_bot/presentation/keyboards/themes.py).

Починка: `from html import escape` и оборачивать пользовательские подстановки. В кнопках
HTML не парсится, но `<` в тексте кнопки лучше тоже не пускать.

## 2. Партии и FSM теряются при рестарте — ПРИНЯТО ОСОЗНАННО

`registry` — обычный `dict` в памяти, FSM — `MemoryStorage`. Рестарт бота во время партии
= партия потеряна. Владелец решил, что это нормально: игра идёт ~20 минут, перезапустить
проще, чем городить персистентность.

Побочный эффект: если бот перезапустится, пока пользователь вводит слово, `state.get_data()`
вернёт пустой словарь, и `data["theme_id"]` бросит `KeyError`
([themes.py:receive_word](../../src/bond_bot/presentation/handlers/themes.py#L221)).
Аккуратный фикс — `data.get("theme_id")` с понятным сообщением, а не персистентность.

## 3. Нет глобального обработчика ошибок

Любое необработанное исключение в хендлере уходит в лог aiogram, пользователь видит
зависшее сообщение и не понимает, что случилось. `Dispatcher.errors` не подключён
([__main__.py](../../src/bond_bot/__main__.py)). Это усиливает проблемы 1 и 2.

## 4. Тема без слов не отфильтрована в одном месте

`show_theme_choice()` отбирает `if t.word_count` — пустые темы в выбор не попадают.
Но кнопка «🎮 Играть с этой темой» из карточки темы проверяет то же самое отдельно
([themes.py:play_theme](../../src/bond_bot/presentation/handlers/themes.py#L379)).
Две независимые проверки одного правила — если добавится третий вход в игру, легко забыть.

## 5. Alembic не инициализирован

Пакет в зависимостях, каталога миграций нет. Схема создаётся `create_all()`. Изменение
модели на живой базе не применится — файл БД придётся удалять. Для проекта на 12 тем
это приемлемо, но при первом же деплое на постоянный хост станет проблемой.

## 6. `KICK_ALL` может выгнать всех живых разом

Если голоса разошлись поровну по всем живым игрокам и хост выбрал «выгнать всех», выбывают
все, `alive` становится пустым, и `not game.alive_spies` даёт победу мирных. Ситуация
крайне редкая и является осознанным выбором хоста, но выглядит странно. Владелец в курсе.

## Незакрытые вопросы (не баги, ждут решения владельца)

- При 2+ шпионах в `DOUBLE_AGENT` каждый тянет своё похожее слово. Обсуждалось; менять на
  «всем шпионам одно слово» — одна строка в `start_game()`, если владелец захочет
- Хостинг и деплой отложены осознанно

## Систематическое наблюдение

Проблемы 1 и 3 — один класс: **нет единой точки, где пользовательский ввод и ошибки
приводятся к безопасному виду перед отправкой в Telegram**. Правильное лечение —
middleware/errors-хендлер, а не точечные заплатки в каждой функции `texts.py`.
