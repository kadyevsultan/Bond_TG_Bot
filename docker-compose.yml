services:
  bot:
    build: .
    image: bond-bot:latest
    container_name: bond-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/bond_bot.sqlite3
    volumes:
      - bond-data:/data
    stop_grace_period: 10s

volumes:
  bond-data:
