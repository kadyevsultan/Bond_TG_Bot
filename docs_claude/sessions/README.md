# Журнал сессий

## Быстрый статус
- **Стек:** Python 3.12 · aiogram 3 · SQLAlchemy 2 (async, SQLite) · Alembic · Poetry · Docker
- **Ветка:** main · коммит 9b14d48
- **Сессий:** 3
- **Тесты:** 128 зелёных, ruff чист
- **Прод:** alwaysdata, служба 28793, деплой через GitHub Actions
- **Обновлено:** 2026-08-17

---

### День 1 — 2026-08-12 | Проектирование, реализация, онбординг

| # | Сессия | Что сделано | Ключевые файлы |
|---|--------|-------------|----------------|
| 001 | [session_001](2026-08-12/session_001.md) · [отчёт](2026-08-12/session_001_REPORT.md) | Сбор требований, движок игры, слой данных, UI партии, редактор тем, 12 тем, онбординг | `domain/engine.py`, `handlers/game.py`, `handlers/themes.py`, `CLAUDE.md` |

**Итог дня:** бот играбелен от начала до конца. Создана трёхслойная система знаний.

**Ключевые решения:** pass-and-play в личке хоста; чистый домен без aiogram и sqlalchemy;
шпион в DOUBLE_AGENT не знает о роли;
шпионов может быть большинство; каталог тем открыт без модерации; статистика отменена;
код без докстрингов и комментариев.

---

### День 2 — 2026-08-13 | Фичи партии, права админа, инфраструктура

| # | Сессия | Что сделано | Ключевые файлы |
|---|--------|-------------|----------------|
| 002 | [session_002](2026-08-13/session_002.md) | Вердикт шпиона вместо списка слов, музыкальный режим, правка встроенных тем админом с корзиной, Alembic, Docker multi-stage, escape, errors-хендлер | `domain/engine.py`, `migrations/env.py`, `presentation/errors.py`, `Dockerfile` |

**Итог дня:** закрыты все HIGH из отчёта по техдолгу; тестов 78 → 128.

**Ключевые решения:** догадку шпиона проверяет хост, а не бот; источник правды после правки
админом — база, не JSON; удаление встроенной темы мягкое (`is_deleted`); миграции живут внутри
пакета. Главный урок: batch-миграция при включённых внешних ключах уносит дочерние строки —
после миграции проверять количество строк, а не только схему.

---

### День 3 — 2026-08-17 | Хостинг, деплой, CI/CD

| # | Сессия | Что сделано | Ключевые файлы |
|---|--------|-------------|----------------|
| 003 | [session_003](2026-08-17/session_003.md) | Исследование бесплатных хостингов, деплой на alwaysdata, GitHub Actions с тестами и rsync-деплоем, маркерный перезапуск службы | `.github/workflows/ci.yml`, `scripts/alwaysdata/*.sh`, `docs_claude/project/deploy/alwaysdata.md` |

**Итог дня:** бот работает 24/7 на alwaysdata бесплатно и без карты, деплой сводится к `git push`.

**Ключевые решения:** alwaysdata вместо serv00 (Linux против FreeBSD снимает сборку
`pydantic-core`); код заливается `rsync` с раннера, поэтому сервер не знает про GitHub; `.env`
пишет CI из секретов через stdin; перезапуск через маркер, чтобы не требовать security key.
